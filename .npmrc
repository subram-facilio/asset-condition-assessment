; @facilio/dsm-react-wrapper (the Facilio design system) is published to the
; internal registry, not npmjs. The auth token lives in ~/.npmrc and is never
; committed — without it `npm install` fails on the @facilio/* packages only.
registry=https://repo.facilio.in/repository/npm-group
